meta {
  name: Send Email Login Link - Invalid Email
  type: http
  seq: 6
}

post {
  url: {{base_url}}/api/auth/email/send
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "email": "not-an-email"
  }
}

tests {
  expect(res.status).to.equal(400)
  expect(res.body).to.have.property("error")
}
