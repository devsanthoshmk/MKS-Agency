meta {
  name: Verify JWT Token - Unauthorized
  type: http
  seq: 9
}

get {
  url: {{base_url}}/api/auth/verify
  body: none
  auth: none
}

tests {
  expect(res.status).to.equal(401)
  expect(res.body).to.have.property("error")
}
