meta {
  name: Guest Login - Missing Fields
  type: http
  seq: 3
}

post {
  url: {{base_url}}/api/auth/guest
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "name": "Test Guest"
  }
}

tests {
  expect(res.status).to.equal(400)
  expect(res.body).to.have.property("error")
}
